---
import Table from "../components/Table.astro";
import { getEntry } from "astro:content";
interface Props {
    equipment: string;
    category: string;
}
const { equipment, category } = Astro.props;
const equips = await getEntry(equipment, category);
---

<Table data={equips?.data} />
<slot />
